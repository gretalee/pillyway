---
name: Waypoints POI feature
description: Backend implementation of the Waypoints POI feature — slug on CaminoPoint, Accommodation/Sight models, WaypointsModule, UploadsModule
type: project
---

The Waypoints POI feature (branch `feature/waypoints-poi`) adds:

- `slug` field (unique, NOT NULL) on `CaminoPoint` — generated by `CaminosService.generateSlug()` at create time, immutable thereafter
- Two Prisma migrations: first adds nullable slug + Accommodation/Sight tables + backfill SQL; second makes slug NOT NULL
- `Accommodation` and `Sight` models linked to `CaminoPoint` via `camino_point_id` (CASCADE delete)
- `WaypointsModule`: `GET /waypoints/:slug` (public), `POST /waypoints/:slug/accommodations|sights` (pilgrim role)
- `UploadsModule`: `POST /uploads/images` (pilgrim role, multer, Supabase Storage, 10 MB × 10 files)
- `StagePointSummary` DTO extended with `slug: string` — propagated through `StagesService.findByCamino()` and `findOne()`
- `CaminoPointInResponse` and `CaminoDetailFull.caminoPoints` extended with `slug: string`

**Why:** Waypoints need stable, human-readable URLs for the frontend to link to POI detail pages.
**How to apply:** When adding new features that touch CaminoPoint, ensure `slug` is included in any response DTO that returns CaminoPoint data. The `generateSlug` private method in CaminosService handles collision resolution (country suffix, then numeric suffix).

New env vars required (see `.env.example`): `SUPABASE_S3_URL`, `SUPABASE_S3_REGION`, `SUPABASE_S3_BUCKET`, `SUPABASE_S3_ACCESS_KEY`, `SUPABASE_S3_SECRET_KEY`.

After checkout: run `yarn install` (for multer) then `yarn workspace @pillyway/backend prisma generate` (for updated Prisma client types).
