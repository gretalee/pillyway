name: CI — Unit Tests
on:
  push:
    branches: [main]
  pull_request:
    branches: ["**"]
jobs:
  test:
    name: Unit Tests (backend + frontend)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version

      - name: Enable Corepack
        run: corepack enable

      - name: Install backend dependencies
        run: yarn --cwd apps/backend install --frozen-lockfile
        env:
          # prisma generate runs via postinstall; it does not need a real DB connection
          DATABASE_URL: ""

      - name: Install frontend dependencies
        run: yarn --cwd apps/frontend install --frozen-lockfile

      - name: Run unit tests
        run: yarn test
